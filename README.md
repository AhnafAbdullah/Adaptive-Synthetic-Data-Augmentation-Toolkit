# Adaptive-Synthetic-Data-Augmentation-Toolkit
A closed-loop toolkit that dynamically generates synthetic tabular data (or images) targeted at a model’s current weaknesses. By analyzing model errors (e.g. on rare classes or outliers), the system synthesizes new data (via GANs, VAEs, copula models) to specifically address those areas, retraining the model in a feedback loop.
